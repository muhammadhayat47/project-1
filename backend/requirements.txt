# CareerOS backend dependencies
# Pinned to match the versions actually verified working in this project's
# local environment (see backend/.venv) — previously this file listed much
# older pins than what was installed, which is a common source of
# "works on my machine" bugs. Re-pin here whenever you deliberately upgrade.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
SQLAlchemy==2.0.52
python-dotenv==1.2.3
pandas==3.0.5
numpy==2.5.3
scikit-learn==1.9.0
pdfplumber==0.11.10
python-docx==1.2.0
requests==2.34.2
openai==3.8.0
email-validator==2.3.0

# Dev / testing
pytest==9.1.1
httpx==0.28.1
ruff==0.8.4
