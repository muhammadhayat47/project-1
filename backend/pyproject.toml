[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]  # line length is handled by formatting, not a hard error

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F401"]
