# The frontend talks to the backend through Vite's dev proxy (see vite.config.js),
# so you don't need to set anything for local development.
# Set this only if you're pointing at a backend running somewhere other than
# http://127.0.0.1:8000 (e.g. a deployed API).
VITE_API_BASE_URL=
